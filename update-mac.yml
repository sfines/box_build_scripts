- tasks:

  - name: Install brew packages
    community.general.homebrew:
      name: "{{ packages }}"
      update_homebrew: true
      vars:
        packages:
          - python@3.9
          - tmux
          - fsf
          - bat
          - the_silver_searcher
          - htop
          - 